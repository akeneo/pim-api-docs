get:
  summary: Get a record media file
  operationId: "get_reference_entity_media_files__code_"
  description: This endpoint allows you to get the information about a given record media file.
  tags:
    - Record media file
  x-versions:
    - "3.0"
  x-ee: true
  parameters:
    - $ref: '#/parameters/code'
  responses:
    200:
      description: OK
      x-details: Returns the content of the record media file in JSON standard format
      schema:
        $ref: '#/definitions/RecordMediaFile'
    401:
      $ref: "#/responses/401Error"
    404:
      $ref: "#/responses/404Error"
    406:
      $ref: "#/responses/406Error"
