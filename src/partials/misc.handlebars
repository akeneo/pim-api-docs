{{!-- Template for the misc documentation pages --}}
{{#> layout }}
    <div class="container">
        <div class="row">
            <div class="col-xs-12">
                <h1>{{{title}}}</h1>
            </div>
        </div>
        <div class="row">
            {{{mainContent}}}
        </div>
    </div>
{{/layout}}
