type: object
required: ["code"]
properties:
  code:
    type: string
    description: Currency code
    x-immutable: true
  enabled:
    type: boolean
    description: Whether the currency is enabled
example: {
  "code": "EUR",
  "enabled": true
}
